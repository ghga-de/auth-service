# This file was autogenerated, please do not modify.
info:
  title: FastAPI
  version: 0.1.0
openapi: 3.0.2
paths:
  /:
    get:
      description: Greet the World
      operationId: greet
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
      summary: Greet the world
  /create_demo_user:
    post:
      description: Create a new user (for demonstration only)
      operationId: demo
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
      summary: Create demo user
